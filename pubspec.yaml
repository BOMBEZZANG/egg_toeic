name: egg_toeic
description: "TOEIC Part 5 Practice App with Gamification"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3

  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1

  # Firebase
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  firebase_analytics: ^10.7.4
  firebase_auth: ^4.15.0

  # Data Models
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

  # UI/UX
  lottie: ^3.0.0
  fl_chart: ^0.66.0

  # Utilities
  uuid: ^4.2.1
  intl: ^0.18.1
  collection: ^1.18.0
  equatable: ^2.0.5

  # Navigation
  go_router: ^13.0.0

  # Icons
  cupertino_icons: ^1.0.8

  # AdMob
  google_mobile_ads: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  riverpod_generator: ^2.3.9
  hive_generator: ^2.0.1
  mockito: ^5.4.4

flutter:
  uses-material-design: true

  # Add asset directories for animations and images
  assets:
    - assets/animations/
    - assets/images/
    - assets/icons/